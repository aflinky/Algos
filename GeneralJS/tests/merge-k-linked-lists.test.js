const func = require('../merge-k-linked-lists');
const imports = require('../../utils/linkedLists');
const { makeLL } = imports;

describe('Merge k Linked Lists', () => {
  test('meow', () => {
    const wow = makeLL([1,2,3,4,5]);
    console.log('wow', wow)
  });
})
